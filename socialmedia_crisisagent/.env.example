# LLM Provider API Keys
# Set at least one of the following keys. The system will try them in order.

# OpenAI (validated at startup)
OPENAI_API_KEY=

# Google Gemini (fallback via litellm)
GEMINI_API_KEY=

# Groq (fallback via litellm)
GROQ_API_KEY=
